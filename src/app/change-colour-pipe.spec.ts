import { ChangeColourPipe } from './change-colour-pipe';

describe('ChangeColourPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeColourPipe();
    expect(pipe).toBeTruthy();
  });
});
